# Auto detect text files and perform LF normalization
* text=auto

# Fixture files carry exact bytes (BOM, CRLF, escapes) that tests depend on.
# Never normalize line endings in them.
tests/fixtures/** -text

# The Pages template is a ZIP; never normalize it.
src/io/pages/style_template.pages binary
