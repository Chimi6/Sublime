[package]
name = "sublime-bench"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
sublime = { path = ".." }
csv = "1"
serde = "1"
serde_json = "1"

[profile.release]
lto = "fat"
codegen-units = 1
opt-level = 3
